# Bank AD SOAP Login (optional - defaults shown)
# AD_SOAP_ENDPOINT=https://10.250.7.210:443/SSO/ADLogin.asmx
# AD_SOAP_ACTION=http://tempuri.org/userAttributes
# AD_VERIFY_SSL=false
# AD_TIMEOUT=30

# [DUMMY USER / TESTING] Local dev only: bypass AD when endpoint unreachable.
# When AD_SKIP=true, any non-empty password is accepted for users in user_account.
# Add test users via Admin > User Management.
# AD_SKIP=true

# Debug: log raw AD SOAP response (set true to diagnose login issues)
# AD_DEBUG=true
